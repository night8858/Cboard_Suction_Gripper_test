# CMSIS-DAP (DAPLink) configuration for STM32F407
adapter driver cmsis-dap
cmsis-dap vid_pid 0x0d28 0x0204

transport select swd

source [find target/stm32f4x.cfg]

adapter speed 4000

reset_config srst_only srst_nogate